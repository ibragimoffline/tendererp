# Tender-AI ERP — backend bog'liqliklari
# O'rnatish:  .venv/Scripts/python.exe -m pip install -r requirements.txt
#
# Tender-AI dagi ro'yxatning KICHIK qismi: ERP'da AI yo'q, fayl yuklash yo'q,
# .xlsx/.pdf o'qish yo'q. Kerak bo'lganda (masalan mijoz hujjatlari uchun
# shablon) openpyxl qo'shiladi — hozircha yo'q.
fastapi>=0.139
uvicorn[standard]>=0.51
psycopg2-binary>=2.9
python-dotenv>=1.2

# Sinov uchun (fastapi.testclient httpx ni talab qiladi)
httpx>=0.27

# Fayl yuklash - mijoz hujjatlari shabloni (FastAPI UploadFile buni TALAB qiladi)
python-multipart>=0.0.20
